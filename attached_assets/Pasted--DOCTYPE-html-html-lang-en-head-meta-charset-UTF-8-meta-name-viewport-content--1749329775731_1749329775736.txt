<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Mistunnel</title>
  <link rel="icon" type="image/png" href="data:image/png;base64,iVBOR..." />
  <style>
    :root {
      --bg-color: #0a0a0a;
      --accent-color: #87ceeb;
      --text-color: #ffffff;
      --sidebar-bg: rgba(15, 15, 15, 0.95);
      --card-bg: rgba(30, 30, 30, 0.8);
      --hover-glow: 0 0 20px rgba(135, 206, 235, 0.5);
    }
    *, *::before, *::after {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: 'Arial', sans-serif;
      background: var(--bg-color);
      color: var(--text-color);
      min-height: 100vh;
      background-image:
        radial-gradient(circle at 20% 80%, rgba(135, 206, 235, 0.1) 0%, transparent 50%),
        radial-gradient(circle at 80% 20%, rgba(135, 206, 235, 0.1) 0%, transparent 50%);
      transition: all 0.3s ease;
    }
    .header {
      display: flex;
      align-items: center;
      padding: 20px;
      background: rgba(10, 10, 10, 0.9);
      backdrop-filter: blur(10px);
      border-bottom: 2px solid var(--accent-color);
    }
    .logo {
      width: 50px;
      height: 50px;
      border-radius: 50%;
      margin-right: 15px;
      transition: transform 0.3s ease;
    }
    .logo:hover {
      transform: scale(1.1) rotate(5deg);
    }
    .site-title {
      font-size: 2.5rem;
      font-weight: bold;
      color: var(--accent-color);
      text-shadow: 0 0 10px rgba(135, 206, 235, 0.5);
    }
    .sidebar-toggle {
      background: var(--accent-color);
      border: none;
      padding: 10px 15px;
      border-radius: 10px;
      color: black;
      font-weight: bold;
      cursor: pointer;
      margin-left: auto;
      transition: all 0.3s ease;
      box-shadow: var(--hover-glow);
    }
    .sidebar-toggle:hover {
      transform: translateY(-2px);
      box-shadow: 0 5px 25px rgba(135, 206, 235, 0.7);
    }
    .sidebar {
      position: fixed;
      top: 0;
      left: -350px;
      width: 350px;
      height: 100vh;
      background: var(--sidebar-bg);
      backdrop-filter: blur(20px);
      padding: 20px;
      transition: all 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
      z-index: 1000;
      border-right: 3px solid var(--accent-color);
      box-shadow: 0 0 50px rgba(135, 206, 235, 0.3);
    }
    .sidebar.open {
      left: 0;
    }
    .sidebar-nav {
      list-style: none;
      margin-top: 60px;
    }
    .sidebar-nav li {
      margin: 15px 0;
    }
    .sidebar-nav a {
      display: block;
      color: var(--text-color);
      text-decoration: none;
      padding: 15px 20px;
      border-radius: 15px;
      transition: all 0.3s ease;
      border: 2px solid transparent;
      position: relative;
      overflow: hidden;
    }
    .sidebar-nav a::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(135, 206, 235, 0.3), transparent);
      transition: left 0.5s ease;
    }
    .sidebar-nav a:hover::before {
      left: 100%;
    }
    .sidebar-nav a:hover {
      background: rgba(135, 206, 235, 0.1);
      border-color: var(--accent-color);
      transform: translateX(10px);
      box-shadow: var(--hover-glow);
    }
    .main-content {
      padding: 40px 20px;
      transition: margin-left 0.4s ease;
    }
    .content-section {
      display: none;
      animation: fadeIn 0.5s ease-in-out;
    }
    .content-section.active {
      display: block;
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .welcome-section {
      text-align: center;
      margin-bottom: 40px;
    }
    .welcome-title {
      font-size: 3rem;
      margin-bottom: 15px;
      background: linear-gradient(45deg, var(--accent-color), #fff);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }
    .welcome-subtitle {
      font-size: 1.2rem;
      color: #ccc;
      margin-bottom: 20px;
    }
    .review-link {
      display: inline-block;
      background: linear-gradient(45deg, var(--accent-color), #4a9eff);
      color: black;
      padding: 12px 25px;
      border-radius: 25px;
      text-decoration: none;
      font-weight: bold;
      transition: all 0.3s ease;
      box-shadow: 0 4px 15px rgba(135, 206, 235, 0.3);
    }
    .review-link:hover {
      transform: translateY(-3px);
      box-shadow: 0 8px 25px rgba(135, 206, 235, 0.5);
    }
    .proxy-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 25px;
      margin-top: 30px;
    }
    .proxy-card {
      background: var(--card-bg);
      padding: 25px;
      border-radius: 20px;
      border: 2px solid transparent;
      transition: all 0.3s ease;
      backdrop-filter: blur(10px);
      position: relative;
      overflow: hidden;
    }
    .proxy-card::before {
      content: '';
      position: absolute;
      top: -2px;
      left: -2px;
      right: -2px;
      bottom: -2px;
      background: linear-gradient(45deg, var(--accent-color), transparent, var(--accent-color));
      border-radius: 20px;
      z-index: -1;
      opacity: 0;
      transition: opacity 0.3s ease;
    }
    .proxy-card:hover::before {
      opacity: 1;
    }
    .proxy-card:hover {
      transform: translateY(-10px);
      box-shadow: var(--hover-glow);
      border-color: var(--accent-color);
    }
    .proxy-card h3 {
      color: var(--accent-color);
      margin-bottom: 15px;
      font-size: 1.5rem;
    }
    .launch-btn {
      background: linear-gradient(45deg, var(--accent-color), #4a9eff);
      color: black;
      border: none;
      padding: 12px 20px;
      border-radius: 10px;
      cursor: pointer;
      font-weight: bold;
      transition: all 0.3s ease;
      width: 100%;
      margin-top: 15px;
    }
    .launch-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(135, 206, 235, 0.4);
    }
    .settings-grid {
      display: grid;
      gap: 20px;
      margin-top: 20px;
    }
    .setting-item {
      background: var(--card-bg);
      padding: 20px;
      border-radius: 15px;
      transition: all 0.3s ease;
      border: 2px solid transparent;
    }
    .setting-item:hover {
      border-color: var(--accent-color);
      box-shadow: var(--hover-glow);
    }
    .setting-toggle {
      display: flex;
      align-items: center;
      justify-content: space-between;
    }
    .toggle-switch {
      position: relative;
      width: 60px;
      height: 30px;
      background: #333;
      border-radius: 15px;
      cursor: pointer;
      transition: all 0.3s ease;
    }
    .toggle-switch.active {
      background: var(--accent-color);
    }
    .toggle-switch::after {
      content: '';
      position: absolute;
      width: 26px;
      height: 26px;
      background: white;
      border-radius: 50%;
      top: 2px;
      left: 2px;
      transition: all 0.3s ease;
    }
    .toggle-switch.active::after {
      left: 32px;
    }
    .color-picker {
      width: 50px;
      height: 30px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }
    .social-links {
      display: flex;
      gap: 20px;
      margin-top: 20px;
      justify-content: center;
    }
    .social-link {
      display: flex;
      align-items: center;
      gap: 10px;
      padding: 15px 20px;
      background: var(--card-bg);
      border-radius: 15px;
      text-decoration: none;
      color: var(--text-color);
      transition: all 0.3s ease;
      border: 2px solid transparent;
    }
    .social-link:hover {
      transform: translateY(-5px);
      border-color: var(--accent-color);
      box-shadow: var(--hover-glow);
    }
    .content-text {
      line-height: 1.6;
      margin-bottom: 15px;
    }
    .content-text h3 {
      color: var(--accent-color);
      margin: 20px 0 10px 0;
    }
    .content-text h4 {
      color: #ccc;
      margin: 15px 0 10px 0;
    }
    .content-text ul {
      margin-left: 20px;
      margin-bottom: 20px;
    }
    .content-text li {
      margin-bottom: 5px;
    }
    .code-block {
      background: #1a1a1a;
      padding: 15px;
      border-radius: 10px;
      margin: 10px 0;
      font-family: 'Courier New', monospace;
      font-size: 0.9rem;
      overflow-x: auto;
      border-left: 4px solid var(--accent-color);
      white-space: pre-wrap;
    }
    .overlay {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.5);
      opacity: 0;
      visibility: hidden;
      transition: all 0.3s ease;
      z-index: 999;
    }
    .overlay.active {
      opacity: 1;
      visibility: visible;
    }
    @media (max-width: 768px) {
      .sidebar {
        width: 100%;
        left: -100%;
      }
      .proxy-grid {
        grid-template-columns: 1fr;
      }
      .social-links {
        flex-direction: column;
      }
    }
  </style>
</head>
<body>
  <div class="overlay" id="overlay"></div>
  <header class="header">
    <img src="data:image/svg+xml,…" alt="Mistunnel Logo" class="logo" />
    <h1 class="site-title">Mistunnel</h1>
    <button class="sidebar-toggle" onclick="toggleSidebar()">☰ Menu</button>
  </header>
  <nav class="sidebar" id="sidebar">
    <ul class="sidebar-nav">
      <li><a href="#" onclick="showSection('home')">🏠 Home</a></li>
      <li><a href="#" onclick="showSection('proxies')">🌐 Proxies</a></li>
      <li><a href="#" onclick="showSection('games')">🎮 Games</a></li>
      <li><a href="#" onclick="showSection('settings')">⚙️ Settings</a></li>
      <li><a href="#" onclick="showSection('how-to-create-links')">🔗 How to Create Links</a></li>
      <li><a href="#" onclick="showSection('exprint3r')">🛠️ Exprint3r</a></li>
      <li><a href="#" onclick="showSection('bookmarks')">📚 Bookmarks</a></li>
    </ul>
  </nav>
  <main class="main-content">
    <section id="home" class="content-section active">
      <div class="welcome-section">
        <h1 class="welcome-title">Welcome to Mistunnel</h1>
        <p class="welcome-subtitle">Your gateway to unrestricted browsing and entertainment</p>
        <a href="https://docs.google.com/forms/…" class="review-link" target="_blank">📝 Review Us Here</a>
      </div>
    </section>
    <section id="proxies" class="content-section">
      <h2>🌐 Available Proxies</h2>
      <div class="proxy-grid">
        <!-- Repeat proxies -->
        <div class="proxy-card">
          <h3>Utopia</h3>
          <p>Fast and reliable proxy service</p>
          <button class="launch-btn" onclick="launchProxy('https://docs.google.com/…')">Launch</button>
        </div>
        <!-- Add more cards similarly -->
      </div>
    </section>
    <section id="games" class="content-section">
      <h2>🎮 Games</h2>
      <div class="proxy-grid">
        <div class="proxy-card">
          <h3>Coming Soon!</h3>
          <p>Games will be available in the next update</p>
          <button class="launch-btn" disabled>Coming Soon</button>
        </div>
      </div>
    </section>
    <section id="settings" class="content-section">
      <h2>⚙️ Settings</h2>
      <div class="settings-grid">
        <div class="setting-item">
          <div class="setting-toggle">
            <span>About:Blank Mode</span>
            <div class="toggle-switch" id="blankToggle" onclick="toggleBlankMode()"></div>
          </div>
          <p style="margin-top:10px;font-size:0.9rem;color:#ccc;">
            Opens the website in an about:blank tab for stealth browsing
          </p>
        </div>
        <div class="setting-item">
          <div class="setting-toggle">
            <span>Anti-Close</span>
            <div class="toggle-switch" id="antiCloseToggle" onclick="toggleAntiClose()"></div>
          </div>
          <p style="margin-top:10px;font-size:0.9rem;color:#ccc;">
            Prevents the browser from closing the tab
          </p>
        </div>
        <div class="setting-item">
          <div class="setting-toggle">
            <span>Background Color</span>
            <input type="color" class="color-picker" id="bgColorPicker" value="#0a0a0a"
                   onchange="changeBgColor(this.value)" />
          </div>
          <p style="margin-top:10px;font-size:0.9rem;color:#ccc;">
            Customize the background color of the website
          </p>
        </div>
        <div class="setting-item">
          <h3>Social Links</h3>
          <div class="social-links">
            <a href="https://discord.gg/sJNe73e3" class="social-link" target="_blank">💬 Discord</a>
            <a href="https://TikTok.com/@80_goth" class="social-link" target="_blank">🎵 TikTok</a>
            <a href="https://Instagram.com/jxza04/" class="social-link" target="_blank">📸 Instagram</a>
          </div>
        </div>
      </div>
    </section>
    <section id="how-to-create-links" class="content-section">
      <h2>🔗 How to Create Links</h2>
      <div class="content-text">
        <h3>Step 1:</h3>
        <p>Sign Up To <a href="https://freedns.afraid.org/signup/" target="_blank" style="color: var(--accent-color)">FreeDNS</a></p>
        <p>(For your email, you could use a <strong>Temporary Email</strong> provider like <a href="https://smailpro.com/" target="_blank" style="color: var(--accent-color)">TempMail</a>)</p>
        <h3>Step 2:</h3>
        <p>After Creating an Account, Now we are going to create a Link. Go to this page in <a href="https://freedns.afraid.org/domain/registry/" target="_blank" style="color: var(--accent-color)">FreeDNS</a></p>
        <h3>Step 3:</h3>
        <p>Click a <strong>subdomain</strong> link that you would like to use. <strong>(e.g. chickenkiller.com)</strong></p>
        <h3>Step 4:</h3>
        <p>Finally, Set the <strong>Destination</strong> to one of the IPs below:</p>
        <div class="code-block">
          Truffled IP: 5.161.110.162
          Astro IP: 104.243.37.85
          … (and so on)
        </div>
      </div>
    </section>
    <section id="exprint3r" class="content-section">
      <h2>🛠️ EXPRINT3R / RING TOOL</h2>
      <div class="content-text">
        <h3>Powershell / GUEST MODE</h3>
        <h3>Mask3r</h3>
        <p>I have GoGuardian and I don't know if this works on other blockers.</p>
        <h4>Requirements:</h4>
        <ul>
          <li>Access to file URLs</li>
          <li>Access to about:blank</li>
        </ul>
        <div class="code-block">
          1. Download this file: https://cdn.discordapp.com/… 
          2. Go to extprint3r.github.io…
          3. Turn off Wi‑Fi, click disable…
        </div>
        <h3>How to use RigTools (UPDATED)</h3>
        <h4>Requirements:</h4>
        <ul>
          <li>A brain</li>
          <li>A Chromebook on 128 or below</li>
          <li>An internet connection</li>
          <li>A blocking extension</li>
        </ul>
        <h4>Links:</h4>
        <div class="code-block">
          devtools://devtools/bundled/devtools_app.html?experiments=true&ws=rigtools.appleflyer.xyz:5506
          … (and others)
        </div>
      </div>
    </section>
    <!-- You can add the bookmarks section similarly if needed -->
  </main>
  <script>
    function toggleSidebar() {
      document.getElementById('sidebar').classList.toggle('open');
      document.getElementById('overlay').classList.toggle('active');
    }
    function showSection(id) {
      document.querySelectorAll('.content-section').forEach(el =>
        el.classList.remove('active')
      );
      document.getElementById(id).classList.add('active');
      toggleSidebar(); // optionally close sidebar after click
    }
    function launchProxy(url) {
      window.open(url, '_blank');
    }
    function toggleBlankMode() {
      const t = document.getElementById('blankToggle');
      t.classList.toggle('active');
      if (t.classList.contains('active')) {
        const w = window.open();
        w.document.write(
          `<base target="_blank"><script>location.replace("${location.href}")<\/script>`
        );
      }
    }
    function toggleAntiClose() {
      const t = document.getElementById('antiCloseToggle');
      t.classList.toggle('active');
      if (t.classList.contains('active')) {
        window.onbeforeunload = () => 'Are you sure you want to leave?';
      } else {
        window.onbeforeunload = null;
      }
    }
    function changeBgColor(color) {
      document.documentElement.style.setProperty('--bg-color', color);
    }
    // close sidebar if clicked on overlay
    document.getElementById('overlay').onclick = toggleSidebar;
  </script>
</body>
</html>

